default:
    extensions:
        Behat\MinkExtension:
            base_url: http://localhost:8000
            goutte: ~
            selenium2: ~

    suites:
        apero:
            contexts: [FeatureContext, AperoContext]
            filters: {tags: '@apero' }
